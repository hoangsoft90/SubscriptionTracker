org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# AGP 9 requires JDK 17+. Gradle picks the JDK from JAVA_HOME, so do NOT
# hard-code a machine-specific path here (it breaks CI runners, e.g. GitHub
# Actions Linux which has no /Library/...).
# Local dev: export JAVA_HOME=<path to JDK 21> (see .project/ai-rules.md).
# CI: actions/setup-java sets JAVA_HOME automatically.
